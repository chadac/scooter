{
  "name": "@kubenix-agent-sandbox/ui",
  "version": "0.0.0",
  "description": "Conversation UI (assistant-ui + AG-UI runtime) for agent sandbox sessions, plus a reusable AG-UI client library.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "//": "Design stage. Expected: @assistant-ui/react, @assistant-ui/react-ag-ui (or the AG-UI runtime), react."
  }
}
