"""Datadog integration for Scooter's broker, as a contrib module.

Everything Datadog-specific lives here: the provider factory, the two-key
credential source, and the settings. Nothing imports the broker app.
"""

CONTRIB_NAME = "datadog"
