[pytest]
asyncio_mode = auto
# tests/ on sys.path so `from conftest import ...` resolves explicitly rather than
# relying on pytest's rootdir insertion (which did NOT fire for this suite).
pythonpath = tests
