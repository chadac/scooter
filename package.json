{
  "name": "kubenix-agent-manager",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "Nix-powered agent platform over the Kubernetes agent-sandbox controller.",
  "workspaces": [
    "lib/ts/scooter-schema",
    "services/agent-host",
    "services/claude-sdk-provider",
    "services/marimo-mcp",
    "services/remote-agent",
    "ui"
  ],
  "scripts": {
    "test": "vitest run --project agent-host --project claude-sdk-provider --project marimo-mcp --project ui --project remote-agent --project byoc-controller --project scooter-schema",
    "test:cluster": "vitest run --project cluster",
    "pretest:e2e": "npm -w services/claude-sdk-provider run build && npm -w services/marimo-mcp run build && npm -w @scooter/schema run build && npm --prefix services/agent-host run build",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
