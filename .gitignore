result
result-*
node_modules/
dist/
.direnv/
*.tsbuildinfo

# .envrc is shared dev config (direnv `use flake`) — track it even though a
# global gitignore may exclude it.
!.envrc

# playwright
test-results/
playwright-report/
.playwright/

# uv-nix generated artifacts
services/*/flake.nix
services/*/flake.lock
services/*/uv.lock
services/*/.venv/

# Local planning / design scratch (untracked)
/todo/
